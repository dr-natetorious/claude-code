/**
 * Feature flag stub for development builds
 * All feature flags are disabled by default
 */

export function feature(name: string): boolean {
  // All features are disabled for this development build
  // Enable them later when modules are available
  return false;
}
